<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yk.rxjava">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".wigedit.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".wigedit.activity.ThreadActivity" />
        <activity android:name=".wigedit.activity.ChangeActivity" />
        <activity android:name=".wigedit.activity.FlowableActivity" />
        <activity android:name=".wigedit.activity.BackgroundActivity" />
        <activity android:name=".wigedit.activity.BufferActivity" />
        <activity android:name=".wigedit.activity.SearchActivity" />
        <activity android:name=".wigedit.activity.RetrofitActivity" />
        <activity android:name=".wigedit.activity.TimerActivity" />
        <activity android:name=".wigedit.activity.RepeatActivity" />
        <activity android:name=".wigedit.activity.FormActivity" />
        <activity android:name=".wigedit.activity.CacheActivity" />
        <activity android:name=".wigedit.activity.ScheduledActivity"></activity>
    </application>

</manifest>